# 再レビュー要求の例

Ownerがbaselineを変更すると、影響するFindingは`requires-rereview`になる。前回のverify結果を流用せず、Reviewerが新baselineに対するrereviewとEvidenceを記録する。

## 次の担当

Reviewerがrereviewを実施し、次handoffを発行する。OwnerもImplementerもReviewを省略して受入へ進めず、正本を直接編集しない。
