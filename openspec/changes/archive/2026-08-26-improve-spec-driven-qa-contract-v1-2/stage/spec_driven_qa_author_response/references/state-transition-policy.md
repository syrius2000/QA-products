# 状態遷移方針

Author Response後のQAケースは`author-response-submitted`、または人間判断が必要な場合の`adjudication-required`に留めます。

`verification-in-progress`への移行は別レビュアーが実施し、`closed`や終端結果の設定はQA閉鎖手順に委ねます。三サイクル上限や未解決High/Criticalの扱いは親Skillの方針を優先します。
