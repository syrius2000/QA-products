# 1. レビュー契約の更新

- [x] 1.1 `SKILL.md`にレビュー開始時のリスクプロファイル確認を追加し、配置範囲・重要度・リアルタイム性・許容データ損失・リソース制約・運用形態を記録する手順を追加する。スキル本文のレビューで全項目が確認できることを検証する
- [x] 1.2 `SKILL.md`にFindingの目的適合性分類と、仕様にないセキュリティ要求を自動格上げしない規則を追加する。分類名と重大度判断の分離を目視確認する
- [x] 1.3 `SKILL.md`に技術判定とリスク裁定の分離、および`fixed-and-verified`、`partially-fixed`、`unverified`、`risk-accepted`、`out-of-scope`の扱いを追加する。リスク受容を技術修正済みと記録しない例を確認する

## 2. プロファイルと記録形式

- [x] 2.1 `references/risk-profiles.md`に`proportional-home`の適用条件、優先事項、除外条件を追加する。家庭内LAN・非安全系・非リアルタイムの代表ケースで選択条件が判定できることを確認する
- [x] 2.2 `references/finding-taxonomy.md`または関連テンプレートに目的適合性分類、技術判定、リスク裁定、未検証状態の記録例を追加する。既存のFinding状態との互換性を確認する
- [x] 2.3 `references/qa-principles.md`に、未検証と不合格の区別、目的に直結するデータ品質の優先、リスク受容の必須項目を追加する。各原則がスキルのClosure Rulesと矛盾しないことを確認する

## 3. リスク受容と証拠の検証

- [x] 3.1 リスク受容テンプレートまたはSKILL.mdに、所有者・理由・適用範囲・管理策・期限または見直しトリガーの必須項目を追加する。項目欠落を受容済みとして扱わないことを確認する
- [x] 3.2 実機未接続・外部サービス未起動のケースを、`unverified`または`evidence-gap`として扱う検証例を追加する。再現した仕様違反と区別されることを確認する
- [x] 3.3 CSV移行による固定値補完、キュー容量超過、再起動時のデータ欠損を目的直結のデータ品質として評価する例を追加する。一般的セキュリティ推奨と異なる優先順位になることを確認する

## 4. 検証と互換性確認

- [x] 4.1 既存の`spec-driven-qa-review`検証テストを実行し、標準ライブラリのみで合格することを確認する
- [x] 4.2 代表4ケース（家庭内LANの未指定認証強化、目的に反するキュー損失、実機未接続、所有者の残余リスク受容）で新しい判定規則を評価し、成果物に結果を記録する
- [x] 4.3 `openspec validate --change spec-driven-qa-proportional-gates`と変更のstatusを実行し、全必須アーティファクトが有効であることを確認する
- [x] 4.4 既存ユーザー差分、既存QAケース履歴、リモート状態に変更がないことを`git diff`と`git status`で確認する
